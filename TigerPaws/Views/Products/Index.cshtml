@model IEnumerable<TigerPaws.Models.Product>

@{
    ViewBag.Title = "Our Products";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



@Html.ActionLink("Add Product", "Create", "Products", new { @class = "btn btn-primary" })


<table id="productTbl" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Genre
            </th>
            <th>
                Description
            </th>
            <th>
                Number In Stock
            </th>
            <th>
                Image
            </th>
            <th></th>
        </tr>
    </thead>
    @* @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img class="img-thumbnail" style="height:40px" src="~/Content/Images/@item.Image" />
                </td>
                <td>
                    @Html.ActionLink(item.Name, "Details", "Products", new { id = item.Id }, null)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Genre.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    <button data-product-id="@item.Id" class="btn btn-link js-delete">Delete</button>
                </td>
            </tr>
        } *@

</table>

<div class="linkButton">
   
        @Html.ActionLink("Export to Csv", "ExportToCsv", null, new { @class = "btn btn-primary" })
  
</div>

<div class="linkButton">
   
        @Html.ActionLink("View Csv", "ViewCsv", null, new { @class = "btn btn-primary" })
   
</div>

<div class="linkButton">
 
        @Html.ActionLink("Upload Csv", "Index", "CSV", null, new { @class = "btn btn-primary" })
  
</div>



@section scripts
{
    <script>
        $(document).ready(function () {
            var table = $("#productTbl").DataTable({
                ajax: {
                    url: "/api/products",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, product) {
                            return "<a href='/products/details/" + product.id + "'>" + product.name + "</a>";
                        }
                    },
                    {
                        data: "genre.name"
                    },
                    {
                        data: "description",

                    },
                    {
                        data: "numberInStock"
                    },
                    {
                        data: "image",
                        render: function (data, type, product, meta) {
                            return '<img  class="img-thumbnail" style="height:40px" alt="Image of ' + product.name + '" src="/Content/Images/' + product.image + '">';
                        }
                    },
                    {
                        data: "id",
                        render: function (data, type, product, meta) {
                            return " <a href='/products/edit/" + product.id + "'>Edit</a> | <button class='btn btn-link js-delete' data-product-id=" + data + ">Delete</button>";

                        }
                    }
                ]

            });

            $("#productTbl").on("click", ".js-delete", function () {
                var button = $(this);
                if (confirm("Are you sure you want to delete this product?")) {
                    $.ajax({
                        url: "/api/products/" + button.attr("data-product-id"),
                        method: "DELETE",
                        success: function () {
                            table.row(button.parents("tr")).remove().draw();
                        }
                    });
                }
            });
        });
    </script>
}
